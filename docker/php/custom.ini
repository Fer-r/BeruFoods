; Custom PHP configuration for file uploads
; Set upload limits to 5MB (slightly higher than our 4MB backend validation)
upload_max_filesize = 5M
post_max_size = 5M

; Increase execution time for file uploads
max_execution_time = 120
max_input_time = 120

; Memory limit
memory_limit = 256M 